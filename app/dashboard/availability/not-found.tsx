import NotFound from "@/components/NotFound";
import { redirect } from "next/navigation";
export default function NotFoundPage() {

  return(
<NotFound error="Errore nel caricamento delle disponibilità." />)}